/**
 * The wordmark, and nothing else.
 *
 * The flower watermark that used to sit behind it is gone — the mark stands on
 * its own now. Typography is untouched: Inter, tracked wide, with the trailing
 * letter-space pulled back so it optically centres.
 */
export default function BrandMark() {
  return (
    <div className="brand-mark">
      <h1 className="brand-word">N.O.U.X</h1>
    </div>
  );
}
